json.extract! military, :id, :name, :rank, :registration, :admission_date, :t_shirt_size, :boot_size, :beret_size, :pants_size, :user_id, :created_at, :updated_at
json.url military_url(military, format: :json)
