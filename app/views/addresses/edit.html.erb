<% provide(:current_page, 'Editar Endereço') %>
<div class="row">
  <div class="col-xs-12">
    <!-- general form elements -->
    <div class="box box-primary">
      <div class="box-header with-border">
        <h3 class="box-title"><%= t('edit_address') %></h3>
      </div>
      <!-- /.box-header -->
      <!-- form start -->
      <div class="box-body">
        <%= render 'form', address: @address %>
      </div>
    </div>
    <!-- /.box -->
  </div>
</div>
<%= link_to t('show'), @address, class: 'btn btn-info' %> |
<%= link_to t('back'), addresses_path, class: 'btn btn-default' %>
